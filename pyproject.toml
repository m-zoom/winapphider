[project]
name = "winapphider"
version = "1.0.0"
description = "Hide and unhide Windows applications from taskbar, system tray, and uninstall list"
requires-python = ">=3.8"
dependencies = [
    "questionary>=2.0",
]

[tool.uv]
dev-dependencies = []
